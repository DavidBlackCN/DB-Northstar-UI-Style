# Northstar UI Prototype Skill 增强提示词

请审查并优化现有 `northstar-ui-prototype` Skill。保留它已有的视觉方向、配色基线、字体策略、圆角节奏和克制动效，不要把 Skill 改写成某个具体产品、框架或项目的专用说明。新增内容应来自可迁移的 UI 设计与前端实现经验，重点提高复杂现有界面中的落地可靠性和验收质量。

## 优化目标

1. 将 Skill 从“视觉风格说明”增强为“视觉决策 + 实现约束 + 浏览器验证”的完整工作方法。
2. 保持 Skill 简洁且有判断力，不堆积常识、固定步骤或针对单一故障的补丁规则。
3. 明确区分绝对约束、推荐默认值和需要依据现有产品决定的事项。
4. 保留用户指定的技术栈、设计系统和交互模型，不为了套用 Northstar 风格而重写已有组件体系。
5. 将详细检查表、状态矩阵或高级兼容策略放入按需读取的 reference，避免让 `SKILL.md` 过长。

## 建议补充的通用原则

### 1. 先理解现有系统，再修改视觉

- 开始设计或实现前，检查页面结构、设计变量、组件库、路由状态、Portal 浮层、响应式断点和已有动效。
- 对现有应用优先复用原生组件、语义状态、图标体系和交互行为，只替换视觉表达，不复制一套平行组件。
- 不要仅凭 class 名或截图推断组件行为。动态界面应结合真实 DOM、计算样式和交互结果确认实现边界。
- 当参考页面与目标产品冲突时，优先保持目标产品的信息架构、功能完整性和操作效率，只迁移可兼容的视觉原则。

### 2. 建立完整的语义 Token，而不只是主题色

- 除品牌强调色外，至少定义并成对维护：页面背景、表面层级、浮层、主要文字、次要文字、弱文字、边界、焦点、成功、警告、危险、信息、选中和禁用状态。
- 每个语义色都应有前景色、柔和背景和边框表达，且在浅色与深色模式下分别校准，不采用简单反色。
- 颜色应表达语义而非组件名称。同一种状态在卡片、Badge、按钮、表格和通知中应保持一致含义。
- 品牌色不应吞并成功、警告和危险状态。状态色需要稳定，不随用户选择的品牌主题失去辨识度。
- 避免在组件规则中散布色值；只有确实不属于语义体系的资产色才允许局部硬编码。

### 3. 按状态矩阵设计组件

- 对交互组件同时考虑 `default / hover / active / focus-visible / selected / disabled / loading`，对数据状态考虑 `success / warning / danger / idle / empty / error`。
- Hover 与 selected 必须能被清楚区分：Hover 是短暂反馈，selected 是持久状态，不能只靠同一种背景色表达。
- 状态不能只依赖颜色。重要状态应结合图标、文字、边框或形状变化，并保证图标与文字同步着色。
- 不要让父容器 Hover 误触发内部控件的选中视觉；状态选择器应绑定真实交互目标或真实状态属性。
- 状态变化后应检查原有透明度、滤镜、过渡和动画是否削弱新颜色或造成短暂闪烁。

### 4. 将浮层视为独立视觉层级

- Tooltip、Popover、Dropdown、Dialog、Drawer、Toast 和确认框通常通过 Portal 渲染，不能假设它们继承页面容器样式。
- 优先复用组件库原生浮层结构、定位器和动效，避免用伪元素复制一个近似版本。
- 浮层需要单独验证背景、文字、边框、箭头、阴影、焦点管理、遮罩、堆叠顺序及浅深模式。
- 同类浮层必须使用一致的间距、圆角、字体和出现方向；不要因触发器来源不同而产生两套视觉语言。

### 5. 控制 CSS 层叠和动态样式风险

- 修改既有应用时，应检查选择器优先级、样式注入顺序、CSS-in-JS 哈希类、内联声明、伪类、过渡和关键帧，而不是盲目增加 `!important`。
- 优先通过语义类、状态属性、作用域和层叠顺序解决覆盖问题。仅在需要跨越第三方组件边界且范围明确时使用 `!important`。
- 通用规则应显式排除更具体的语义状态，避免后续规则把成功、警告、危险或选中状态重新覆盖为中性色。
- 动态添加的类或样式必须可重复执行且幂等；组件卸载、路由切换或状态恢复时不得遗留错误状态。
- MutationObserver、定时协调器或全局补丁只用于无法通过组件接口解决的问题，并应限制观察范围、避免重复绑定和无休止重绘。

### 6. 区分 DOM、Canvas、WebGL 和编辑器渲染面

- 字体、颜色或主题可能由 DOM CSS、Canvas、WebGL、终端、代码编辑器或图表库分别控制。不要假设修改容器 CSS 就能改变实际绘制结果。
- 对非 DOM 渲染组件，优先使用其公开配置、主题 API 或实例选项；只有缺少接口时才考虑受限且可回退的桥接方案。
- 修改渲染器或全局原型前，必须限制作用域并检查是否影响图表、编辑器、终端或其他画布组件。
- 字体要求应验证字体资源已加载、实际渲染器使用了目标字体、字重存在，并检查中英文回退和等宽对齐。

### 7. 让排版和密度适配信息类型

- 不要仅通过缩小字体解决空间问题。先调整容器宽度、信息分组、行高、间距和内容优先级。
- 紧凑工具栏、侧栏、表格和卡片中的标题应使用适合容器的字号；避免页面显空时无差别放大所有文字。
- 长正文、说明和中文段落保持 Sans Serif；Mono 用于代码、路径、ID、版本、数值、短标签和技术标题。
- 数字型指标使用 tabular figures，并为动态数字、Badge、工具栏和固定格式组件预留稳定尺寸，避免状态切换引发布局跳动。
- 内容宽度应由阅读长度和信息密度决定。Dashboard、表格、编辑器和文档页面不应共享同一个最大宽度。

### 8. 动效服务于状态连续性

- 动效优先解释状态变化、空间关系和操作反馈，不为静态内容添加装饰性入场。
- 颜色主题切换应让页面内容始终可见，并确保相关元素同步过渡；避免遮罩、纯色中间帧或分批切换造成闪烁。
- 高频交互使用短、可中断的颜色、边框、透明度或小位移动效。持久选择态不应依赖仍在运行的动画维持。
- 新样式写入时检查旧 transition 是否从错误起点播放、长时间停滞或反复被动态协调器重启。
- 所有关键动效都应有 `prefers-reduced-motion` 静态退化方案。

### 9. 响应式设计验证真实工作流

- 响应式不只是缩小桌面布局。需要检查导航、工具栏、分页、表格、弹窗、长文本、触摸目标和横向滚动。
- 固定格式组件使用明确的 `min/max`、网格轨道、宽高比或容器约束，避免内容变化时尺寸漂移。
- 横向滚动只能出现在确实需要的内容区域，不应由侧栏、页面壳或浮层意外产生。
- 移动端优先缩减空白和重排信息，不缩小可点击区域或隐藏关键操作。

### 10. 以浏览器可观察结果作为验收依据

- 不以“CSS 已写入”或“类名已添加”作为完成标准，应验证最终计算样式、几何尺寸、渲染结果和交互行为。
- 至少在浅色和深色、桌面和移动视口、键盘和指针交互下检查主要组件。
- 对可配置主题，验证默认主题以及最容易产生对比度问题的替代主题，而不是只看单一配色。
- 检查 Portal 浮层、首次使用引导、加载、空状态、错误状态、二次确认和设计/编辑模式等不常驻界面。
- 对动态状态进行真实切换，确认样式在路由切换、数据刷新、重新挂载和主题切换后仍然正确。
- 使用截图检查整体构图和重叠，使用计算样式检查字体、颜色、边框和透明度；两者不能互相替代。
- 验收时记录无法触发或无法验证的状态，不要用静态推断冒充浏览器验证。

## 建议调整 Skill 的组织

- 保持 `SKILL.md` 只包含：适用范围、视觉基线、核心决策原则、实施流程和验证入口。
- 将详细视觉 Token、状态矩阵和组件规范放入 `references/design-system.md`。
- 将浏览器验收矩阵、计算样式检查方法和非 DOM 渲染检查放入 `references/validation.md`。
- 将不同页面类型的密度与排版策略放入 `references/page-patterns.md`，并只在对应任务中读取。
- 如果 `STYLE_NOTES.md` 是必需资源，应将它放在 Skill 目录内或在 Skill 中明确可解析的位置和缺失时的回退策略，避免依赖调用项目恰好存在同名文件。
- 删除与 `STYLE_NOTES.md` 重复的描述，确保每条规则只有一个权威来源。
- 不需要为这些原则新增脚本，除非未来存在可重复、可确定执行的视觉回归检查流程。

## 对现有 Skill 文案的具体改进

- 扩展 description，使其明确适用于“原型设计和既有界面的主题化/视觉整合”，同时排除纯品牌设计、纯动效审查和无需 UI 的任务。
- 将“原生单文件 HTML”从绝对前提改为该 Skill 的一种常见工作模式；当目标是既有应用时，应遵循原项目组织方式。
- 将具体配色值保留为 Northstar 默认值，但明确用户品牌、现有设计系统或可访问性要求优先。
- 将星点、云朵等主题开关表现降为推荐的 Northstar 示例，而非所有页面必须实现的要求。
- 保留 Maple Mono 的使用边界，但增加“实际渲染器和字体加载验证”，避免只检查 CSS 声明。
- 将当前较简略的验证清单升级为按主题、视口、输入方式、组件状态和渲染技术分类的矩阵。

## 写作要求

- 使用直接、可执行、具有判断标准的中文指令。
- 不引用任何具体业务系统、项目目录、组件哈希、版本号、故障截图或一次性修复方式。
- 不把某一次实现中的偶发现象写成普遍必须遵守的绝对规则。
- 避免重复 Codex 已具备的一般编程能力，只保留会改变设计判断、实现路径或验收标准的信息。
- 完成修改后运行 Skill 校验，并检查 frontmatter、引用路径、渐进披露结构和适用范围是否准确。
